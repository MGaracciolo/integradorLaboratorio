doctype html
html(lang='es')
  head
    meta(charset='UTF-8')
    meta(name='viewport', content='width=device-width, initial-scale=1.0')
    title muestra
    link(rel="stylesheet", href="/stylesheets/style.css")
    
  body
    #cabecera
      img(src='/images/banner.png', alt='Logo del laboratorio')
    #botones
      button#botonAgregarPaciente.custom-button Agregar Paciente
      button#botonBuscarPaciente.custom-button Buscar Paciente
      button#botonInicio.custom-button Inicio
      button.custom-button Orden de Trabajo     
      button.custom-button Salir
    
    h2 Nueva Muestra

    #contenedorformularioMuestra
        form#formularioMuestra(style='display: block;', action='/rutaMuestra/agregar', method='POST')
            //- p.imprimir
            //-     | Id Muestra: 
            //-     input(type="text"  class="imprimir" readonly)
            p.imprimir
                | Fecha de Recoleccion: 
                input(type="date" name="fecha_recoleccion" class="imprimir" required)
            p.imprimir
                | Hora de Recoleccion:
                input(type="time" name="hora_recoleccion" class="imprimir" required)
            form(action="/rutaMuestra/buscarMuestra", method="get")
              p.imprimir
                  | Tipo de Muestra:
                  select(name="tipo_muestra")
                      each tipo_muestra in tipos_muestra
                        option(value=tipo_muestra.id_tipo_muestra)= tipo_muestra.valor
            p
                | Precedencia:
                select(name="precedencia" required)
                    option(value="Local") Local
                    option(value="Externa") Externa 
            p
                | Orden:
                input(type="text" name="id_orden" class="imprimir" readonly value=id)
            button#botonAgregarMuestra(type="submit") Guardar e Imprimir


    footer#footer
      p Rivadavia 548, San Luis, Argentina
      p Teléfono: 266 4000000
      p Email: labwm@gmail.com
      p &copy; 2023 TreeLabWM

    script.
          document.addEventListener('DOMContentLoaded', function() {
            let imprimirBtn = document.getElementById('botonAgregarMuestra');
            if (imprimirBtn) {
              imprimirBtn.addEventListener('click', function() {
                alert('Muestra guardada')
                window.print();
              });
            }
          });
